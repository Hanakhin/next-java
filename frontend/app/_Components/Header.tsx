import {CustomNavbar} from "@/app/_Components/Navbar";

export const Header=()=>{

    return(
        <header className={'fixed inline-flex w-full'}>
                <CustomNavbar/>
        </header>

    )
}